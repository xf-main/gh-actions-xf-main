name: discover

on:
  repository_dispatch:
    types: [discover]

permissions:
  contents: read

jobs:
  discover:
    uses: gh-actions-shared/gh-actions-shared/.github/workflows/discover-core.yml@7a1a6b6755afced9fa538a728a6c7e7a713a3d8c
    with:
      target-org: ${{ github.repository_owner }}
      event-context: ${{ toJson(github.event) }}
      event-name: ${{ github.event_name }}
      expected-event-action: discover
      shared-repo: gh-actions-shared/gh-actions-shared
      shared-ref: 7a1a6b6755afced9fa538a728a6c7e7a713a3d8c
      bws-version: ${{ vars.BWS_VERSION }}
      bws-sha256: ${{ vars.BWS_SHA256 }}
    secrets:
      BWS_ACCESS_TOKEN: ${{ secrets.BWS_ACCESS_TOKEN }}
      BWS_PROJECT_ID: ${{ secrets.BWS_PROJECT_ID }}
